/* file: 85.cf generates intermediate code for assignments and expressions 
   this code should generate 7 instructions of 3-address code 
   you do not need to assign an address to temporaries but you do need to
   assign type and size - strong type is enforced by syntax rules */

float y = 5.5;
char c;
int ret = 0;
int foo ( float f, int i) 
{
    i =  i + 1;
    f = y * 7.0 + 3.5;  
    return ret;  
}
