type
   $ make clean   # remove cf *.o 
   $ make

This is what the makefile does:

flex scan.l                  # generate lex.yy.c
yacc --debug -v -d parse.y   # generate y.tab.c with debugging flags
gcc -c symtab.c              # compile the symbol table
gcc -c y.tab.c               # compiling y.tab.c
gcc -c lex.yy.c              # compile lex.yy.c
gcc -o cfc lex.yy.o y.tab.o symtab.o -lfl  # link lex.yy.o  y.tab.o  symtab.o 


To run the compiler:

   $ ./cfc good.cf 2> good.out 
